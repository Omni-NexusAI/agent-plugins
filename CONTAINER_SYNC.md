# Plugin Source Sync State

## Current Source Contract

- Source checkout: `C:\agent-zero`, release branch `agentspine-099-release`.
- Spine base: Agent Zero v2.7 digest
  `sha256:a7f5ccf351d612c3eaafc708251490277e1d731afe71a2eaf8aa3d871a55ece4`.
- Synced packages: `_enhanced_speech`, `provider_profiles`,
  `ai_link_bridge`, `_agentspine_identity`, and `_multi_source_updater`.

The current packages are plugin-local implementations. They are intended to
survive compatible Agent Zero updates without modifying host-core files.

## Browser Session Sync Compatibility Source

`plugins/browser_session_sync` is synchronized to the standalone
`Omni-NexusAI/browser_session_sync` compatibility source at commit
`45ecd50c1475cd650f4154a730d416b36ee9b05c`, packaged as version 1.4.2 with
deferred follow-ups in `TODO.md`. This includes
Agent Zero 2.11 native persistence and shared-runtime handling plus the
synchronous startup/agent initialization fixes. The package files match that
tested source; `index.yaml` retains this monorepository's repository link.

Validation includes 57 isolated browser/speech regression tests, a successful
live boot with Browser Session Sync enabled and a healthy API response, and
the user's subsequent acceptance checks. This deliberately upgrades the
browser package beyond the historical container snapshots below.

## A0 and Spine Boundaries

- `_enhanced_speech`, `provider_profiles`, and `ai_link_bridge` are portable
  custom-plugin packages for compatible A0 hosts.
- `_agentspine_identity` and `_multi_source_updater` are Spine-only sources.
  Identity remains inert unless the explicit Spine 9.9 environment gate is
  present; neither package is an A0 custom deployment.
- Provider Profiles keeps its A0-specific default configuration and settings UI
  in addition to the synchronized runtime extension and canonical blue-server
  thumbnail.

## Historical GPU-pre Reference

The `agentspine-gpu-pre` container snapshot from 2026-06-15 remains historical
behavioral evidence only. Its copied host overlays are intentionally absent from
all deployable plugin packages. Do not restore or apply `overrides/a0` payloads
to an Agent Zero or Agent Spine runtime.

`_enhanced_mcp_config` is retained only as that historical snapshot and is
excluded from new migration and release copies.

## Maintenance Rules

- Sync source changes from the declared current plugin contract, not from a
  runtime volume or an archived container snapshot.
- Keep manifests, README files, AGENTS.md instructions, and assets aligned.
- Do not commit generated bytecode, logs, secrets, or container snapshots.
